## MATLAB 代码检查说明

针对提供的 `fmincon` 求解片段，主要发现如下问题：

1. **整数决策变量未被约束**  
   代码在目标函数与非线性约束中对 `x(1)`、`x(2)` 使用 `round`，但线性约束 `A, b` 以及上下界只作用于连续变量。求解得到的连续解在四舍五入后可能超过 `eps` 或上界，导致求得的整数解并不满足原本的限制。应改用整数规划（如 `intlinprog`）或在约束中同步使用四舍五入/上取整后的变量。

2. **连续优化器不适合离散问题**  
   `fmincon` 假定问题可微并使用梯度信息，四舍五入会造成不可导/分段常数的目标与约束，优化器可能停在任意位置甚至给出不可行的整数结果。若变量必须为整数，应使用支持整数约束的求解器。

3. **ε 扫描约束与取整不一致**  
   `A = [1 0 0; 0 1 0]`、`b = [eps; eps]` 约束的是连续变量 `x(1), x(2) ≤ eps`，但最终使用的是 `round(x(1))`、`round(x(2))`。例如 `x(1)=eps-0.49` 会被四舍五入成 `eps`，而 `x(1)=eps+0.49` 仍满足线性约束却被四舍五入成 `eps+1`，违反了扫描上限。需要在约束侧同步对取整后的值进行限制，或直接将变量视为连续量，不再取整。

若继续使用 `fmincon`，可以去掉 `round` 将问题当作连续优化；若必须整数，推荐改用 `intlinprog` 并显式声明整数索引。
